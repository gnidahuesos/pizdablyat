# LocalVault Desktop UI (React)

This folder contains the React-based glassmorphism UI shell intended for desktop packaging (Tauri/Electron).

## Dev Setup
```bash
npm install
npm run dev
```

> Note: In this environment, npm registry access may be blocked. If installs fail, run in a local dev machine.
